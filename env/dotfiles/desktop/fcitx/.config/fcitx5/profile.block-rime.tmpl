# 默认输入法组：keyboard-us + rime（Super+Space 切换）；DefaultIM 为登录后的初始输入法。
# fcitx5 运行时会按内存状态重写 profile 并丢弃 block 标记，reconfigure 会重新注入本 block，
# 因此声明式的组定义在每次 reconfigure 后重新成为权威状态。
[Groups/0]
Name=Default
Default Layout=us
DefaultIM=keyboard-us

[Groups/0/Items/0]
Name=keyboard-us
Layout=

[Groups/0/Items/1]
Name=rime
Layout=

[GroupOrder]
0=Default
