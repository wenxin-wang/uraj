# ShowInputMethodInformation: 切换输入法时在输入面板 aux-up 区域显示当前输入法名。
# Wayland 下输入面板由合成器 (niri) 经 zwp_input_popup_surface_v2 定位在文本光标处，
# 即“跟随光标”的提示。noctalia 的 keyboard-layout OSD 已关闭（见 noctalia config.toml）。
[Behavior]
ShowInputMethodInformation=True
